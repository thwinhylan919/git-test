define(["text!./account-access.html", "./account-access", "text!./account-access.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});