define(["text!./review-location-update.html", "./review-location-update", "text!./review-location-update.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});