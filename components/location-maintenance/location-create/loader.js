define(["text!./location-create.html", "./location-create", "text!./location-create.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});