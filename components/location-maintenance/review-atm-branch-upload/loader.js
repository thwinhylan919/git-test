define(["text!./review-atm-branch-upload.html", "./review-atm-branch-upload", "text!./review-atm-branch-upload.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});