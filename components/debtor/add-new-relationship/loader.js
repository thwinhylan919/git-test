define(["text!./add-new-relationship.html", "./add-new-relationship", "text!./add-new-relationship.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});