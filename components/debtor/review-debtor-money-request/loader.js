define(["text!./review-debtor-money-request.html", "./review-debtor-money-request", "text!./review-debtor-money-request.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});