define(["text!./apply-new-facility-landing.html","./apply-new-facility-landing", "baseModel"], function ( template,viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template:template
  };
});
