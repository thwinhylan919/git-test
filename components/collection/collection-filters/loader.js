define(["text!./collection-filters.html", "./collection-filters", "text!./collection-filters.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});