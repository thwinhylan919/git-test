define(["text!./list-collection.html", "./list-collection", "text!./list-collection.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});