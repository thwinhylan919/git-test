define(["text!./view-user-security-question.html", "./view-user-security-question", "text!./view-user-security-question.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});