define(["text!./resource-server-edit.html", "./resource-server-edit", "text!./resource-server-edit.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});