define(["text!./resource-server-review.html", "./resource-server-review", "text!./resource-server-review.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});