define(["text!./identity-domain-search.html", "./identity-domain-search", "text!./identity-domain-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});