define(["text!./resource-server-search.html", "./resource-server-search", "text!./resource-server-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});