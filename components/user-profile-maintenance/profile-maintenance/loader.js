define(["text!./profile-maintenance.html", "./profile-maintenance", "text!./profile-maintenance.json"], function (template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});