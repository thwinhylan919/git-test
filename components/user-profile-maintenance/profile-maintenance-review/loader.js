define(["text!./profile-maintenance-review.html", "./profile-maintenance-review", "text!./profile-maintenance-review.json"], function (template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});
