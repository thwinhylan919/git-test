define(["text!./profile-maintenance-search.html", "./profile-maintenance-search", "text!./profile-maintenance-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});
