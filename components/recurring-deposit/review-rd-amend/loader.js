define(["text!./review-rd-amend.html", "./review-rd-amend", "text!./review-rd-amend.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});