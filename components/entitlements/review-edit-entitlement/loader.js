define(["text!./review-edit-entitlement.html", "./review-edit-entitlement", "text!./review-edit-entitlement.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});