define(["text!./entitlements-search.html", "./entitlements-search", "text!./entitlements-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});