define(["text!./edit-entitlement.html", "./edit-entitlement", "text!./edit-entitlement.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});