define(["text!./entitlements-search-results.html", "./entitlements-search-results", "text!./entitlements-search-results.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});