define(["text!./configuration-details-view.html", "./configuration-details-view", "text!./configuration-details-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});