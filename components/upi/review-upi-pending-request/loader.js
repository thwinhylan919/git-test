define([
    "text!./review-upi-pending-request.html",
    "./review-upi-pending-request"
], function (template, viewModel) {
    "use strict";

    return {
        viewModel: viewModel,
        template: template
    };
});