define(["text!./password-validation.html", "./password-validation", "text!./password-validation.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});