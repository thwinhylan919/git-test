define(["text!./preference-search.html", "./preference-search", "text!./preference-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});