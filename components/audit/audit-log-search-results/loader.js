define(["text!./audit-log-search-results.html", "./audit-log-search-results", "text!./audit-log-search-results.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});