define(["text!./review-update.html", "./review-update", "text!./review-update.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});