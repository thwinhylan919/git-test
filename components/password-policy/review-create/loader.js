define(["text!./review-create.html", "./review-create", "text!./review-create.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});