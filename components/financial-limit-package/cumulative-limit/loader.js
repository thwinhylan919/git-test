define(["text!./cumulative-limit.html", "./cumulative-limit", "text!./cumulative-limit.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});