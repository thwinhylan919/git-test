define(["text!./relationship-matrix-base.html", "./relationship-matrix-base", "text!./relationship-matrix-base.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});