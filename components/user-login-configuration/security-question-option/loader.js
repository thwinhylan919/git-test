define(["text!./security-question-option.html", "./security-question-option", "text!./security-question-option.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});