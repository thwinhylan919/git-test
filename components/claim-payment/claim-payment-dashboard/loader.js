define(["text!./claim-payment-dashboard.html", "./claim-payment-dashboard", "text!./claim-payment-dashboard.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});