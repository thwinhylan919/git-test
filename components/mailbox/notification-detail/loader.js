define(["text!./notification-detail.html", "./notification-detail", "text!./notification-detail.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});