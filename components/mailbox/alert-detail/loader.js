define(["text!./alert-detail.html", "./alert-detail", "text!./alert-detail.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});