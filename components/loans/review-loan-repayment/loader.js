define(["text!./review-loan-repayment.html", "./review-loan-repayment", "text!./review-loan-repayment.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});