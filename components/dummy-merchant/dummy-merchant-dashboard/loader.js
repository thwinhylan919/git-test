define(["text!./dummy-merchant-dashboard.html", "./dummy-merchant-dashboard", "text!./dummy-merchant-dashboard.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});