define(["text!./review-international-payee.html", "./review-international-payee", "text!./review-international-payee.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});