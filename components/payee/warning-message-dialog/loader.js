define(["text!./warning-message-dialog.html", "./warning-message-dialog", "text!./warning-message-dialog.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});