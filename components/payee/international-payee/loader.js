define(["text!./international-payee.html", "./international-payee", "text!./international-payee.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});