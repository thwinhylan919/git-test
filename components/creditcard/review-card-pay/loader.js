define(["text!./review-card-pay.html", "./review-card-pay", "text!./review-card-pay.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});