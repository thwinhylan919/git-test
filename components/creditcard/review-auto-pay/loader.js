define(["text!./review-auto-pay.html", "./review-auto-pay", "text!./review-auto-pay.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});