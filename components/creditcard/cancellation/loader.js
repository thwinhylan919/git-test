define(["text!./cancellation.html", "./cancellation", "text!./cancellation.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});