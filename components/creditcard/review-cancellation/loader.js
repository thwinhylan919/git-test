define(["text!./review-cancellation.html", "./review-cancellation", "text!./review-cancellation.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});