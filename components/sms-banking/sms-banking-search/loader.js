define(["text!./sms-banking-search.html", "./sms-banking-search", "text!./sms-banking-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});