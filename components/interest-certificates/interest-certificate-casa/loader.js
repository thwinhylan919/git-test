define(["text!./interest-certificate-casa.html", "./interest-certificate-casa", "text!./interest-certificate-casa.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});