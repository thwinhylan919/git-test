define(["text!./interest-certificate-loans.html", "./interest-certificate-loans", "text!./interest-certificate-loans.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});