define(["text!./balance-certificate.html", "./balance-certificate", "text!./balance-certificate.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});
