define(["text!./review-statement-request.html", "./review-statement-request", "text!./review-statement-request.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});