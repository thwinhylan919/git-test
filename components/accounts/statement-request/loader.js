define(["text!./statement-request.html", "./statement-request", "text!./statement-request.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});