define(["text!./physical-statement.html", "./physical-statement", "text!./physical-statement.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});