define(["text!./review-payment.html", "./review-payment", "text!./review-payment.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});