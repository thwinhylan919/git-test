define(["text!./social-media.html", "./social-media", "text!./social-media.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});