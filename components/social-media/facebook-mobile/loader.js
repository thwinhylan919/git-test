define(["text!./facebook-mobile.html", "./facebook-mobile", "text!./facebook-mobile.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});