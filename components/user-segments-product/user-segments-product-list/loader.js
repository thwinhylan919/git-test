define(["text!./user-segments-product-list.html", "./user-segments-product-list"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});