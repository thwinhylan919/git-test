define(["text!./cheque-status-inquiry.html", "./cheque-status-inquiry", "text!./cheque-status-inquiry.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});