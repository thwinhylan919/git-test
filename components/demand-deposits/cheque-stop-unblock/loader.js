define(["text!./cheque-stop-unblock.html", "./cheque-stop-unblock", "text!./cheque-stop-unblock.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});