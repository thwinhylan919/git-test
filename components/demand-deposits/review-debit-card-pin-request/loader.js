define(["text!./review-debit-card-pin-request.html", "./review-debit-card-pin-request", "text!./review-debit-card-pin-request.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});