define(["text!./review-debit-card-hotlisting.html", "./review-debit-card-hotlisting", "text!./review-debit-card-hotlisting.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});