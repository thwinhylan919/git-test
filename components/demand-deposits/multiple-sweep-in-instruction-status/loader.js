define(["text!./multiple-sweep-in-instruction-status.html", "./multiple-sweep-in-instruction-status"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});