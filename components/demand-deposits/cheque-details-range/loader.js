define(["text!./cheque-details-range.html", "./cheque-details-range", "text!./cheque-details-range.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});