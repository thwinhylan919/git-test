define(["text!./other-transactions-approval.html", "./other-transactions-approval", "text!./other-transactions-approval.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});