define(["text!./user-group-type.html", "./user-group-type", "text!./user-group-type.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});