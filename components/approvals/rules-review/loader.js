define(["text!./rules-review.html", "./rules-review", "text!./rules-review.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});