define(["text!./user-group-list.html", "./user-group-list", "text!./user-group-list.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});