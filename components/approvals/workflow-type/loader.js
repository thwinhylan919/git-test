define(["text!./workflow-type.html", "./workflow-type", "text!./workflow-type.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});