define(["text!./user-group-review.html", "./user-group-review", "text!./user-group-review.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});