define(["text!./rules-search.html", "./rules-search", "text!./rules-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});