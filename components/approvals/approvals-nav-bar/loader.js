define(["text!./approvals-nav-bar.html", "./approvals-nav-bar", "text!./approvals-nav-bar.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});