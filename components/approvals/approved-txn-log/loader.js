define(["text!./approved-txn-log.html", "./approved-txn-log", "text!./approved-txn-log.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});