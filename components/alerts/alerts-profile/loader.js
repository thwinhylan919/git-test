define(["text!./alerts-profile.html", "./alerts-profile", "text!./alerts-profile.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});