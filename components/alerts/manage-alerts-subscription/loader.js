define(["text!./manage-alerts-subscription.html", "./manage-alerts-subscription", "text!./manage-alerts-subscription.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});