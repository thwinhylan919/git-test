define(["text!./alerts-select-user.html", "./alerts-select-user", "text!./alerts-select-user.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});