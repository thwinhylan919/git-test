define(["text!./review-alerts-subscription.html", "./review-alerts-subscription", "text!./review-alerts-subscription.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});