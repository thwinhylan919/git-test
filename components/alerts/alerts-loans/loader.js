define(["text!./alerts-loans.html", "./alerts-loans", "text!./alerts-loans.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});