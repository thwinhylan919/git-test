define(["text!./alerts-action.html", "./alerts-action", "text!./alerts-action.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});