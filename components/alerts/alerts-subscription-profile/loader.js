define(["text!./alerts-subscription-profile.html", "./alerts-subscription-profile", "text!./alerts-subscription-profile.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});