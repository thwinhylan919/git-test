define(["text!./alerts-payments.html", "./alerts-payments", "text!./alerts-payments.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});