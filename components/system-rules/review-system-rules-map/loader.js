define(["text!./review-system-rules-map.html", "./review-system-rules-map", "text!./review-system-rules-map.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});