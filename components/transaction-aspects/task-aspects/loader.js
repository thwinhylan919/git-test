define(["text!./task-aspects.html", "./task-aspects", "text!./task-aspects.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});