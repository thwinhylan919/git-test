define(["text!./user-access-exclusion.html", "./user-access-exclusion", "text!./user-access-exclusion.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});