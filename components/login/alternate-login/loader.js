define(["text!./alternate-login.html", "./alternate-login", "text!./alternate-login.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});