define(["text!./create-relationship-mapping.html", "./create-relationship-mapping"],
  function(template, viewModel) {
    "use strict";

    return {
      viewModel: viewModel,
      template: template
    };
  });