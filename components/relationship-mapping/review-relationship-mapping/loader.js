define(["text!./review-relationship-mapping.html", "./review-relationship-mapping"],
  function(template, viewModel) {
    "use strict";

    return {
      viewModel: viewModel,
      template: template
    };
  });