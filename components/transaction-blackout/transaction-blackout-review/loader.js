define(["text!./transaction-blackout-review.html", "./transaction-blackout-review", "text!./transaction-blackout-review.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});