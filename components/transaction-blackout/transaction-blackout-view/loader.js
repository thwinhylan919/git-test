define(["text!./transaction-blackout-view.html", "./transaction-blackout-view", "text!./transaction-blackout-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});