define(["text!./review-td-amend.html", "./review-td-amend", "text!./review-td-amend.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});