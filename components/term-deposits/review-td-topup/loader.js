define(["text!./review-td-topup.html", "./review-td-topup", "text!./review-td-topup.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});