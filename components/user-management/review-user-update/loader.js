define(["text!./review-user-update.html", "./review-user-update", "text!./review-user-update.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});