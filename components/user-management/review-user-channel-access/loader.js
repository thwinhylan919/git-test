define(["text!./review-user-channel-access.html", "./review-user-channel-access", "text!./review-user-channel-access.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});