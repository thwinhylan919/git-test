define(["text!./users-search.html", "./users-search", "text!./users-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});