define(["text!./accessible-entity.html", "./accessible-entity", "text!./accessible-entity.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});