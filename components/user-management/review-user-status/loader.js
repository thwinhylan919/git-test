define(["text!./review-user-status.html", "./review-user-status", "text!./review-user-status.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});