define(["text!./application-roles-search.html", "./application-roles-search", "text!./application-roles-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});