define(["text!./financial-type-maintenance.html", "./financial-type-maintenance"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});
