define(["text!./child-party-validate.html", "./child-party-validate", "text!./child-party-validate.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});