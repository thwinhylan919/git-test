define(["text!./linkage-review.html", "./linkage-review", "text!./linkage-review.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});