define(["text!./wallets-user-activity-report.html", "./wallets-user-activity-report", "text!./wallets-user-activity-report.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});