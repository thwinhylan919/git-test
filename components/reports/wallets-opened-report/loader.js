define(["text!./wallets-opened-report.html", "./wallets-opened-report", "text!./wallets-opened-report.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});