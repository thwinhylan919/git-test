define(["text!./user-creation-report.html", "./user-creation-report", "text!./user-creation-report.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});