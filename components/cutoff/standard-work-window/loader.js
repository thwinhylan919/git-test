define(["text!./standard-work-window.html", "./standard-work-window", "text!./standard-work-window.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});