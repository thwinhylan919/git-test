define(["text!./cutoff-exceptions.html", "./cutoff-exceptions", "text!./cutoff-exceptions.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});