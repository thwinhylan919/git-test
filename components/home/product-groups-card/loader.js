define(["text!./product-groups-card.html", "./product-groups-card", "text!./product-groups-card.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});