define(["text!./add-to-favorite-base.html", "./add-to-favorite-base", "text!./add-to-favourite-base.json"], function(template, viewModel) {
    "use strict";

    return {
      viewModel: viewModel,
      template: template
    };
  });