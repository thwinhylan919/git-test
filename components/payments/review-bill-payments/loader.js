define(["text!./review-bill-payments.html", "./review-bill-payments", "text!./review-bill-payments.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});