define(["text!./review-scheduled-payments.html", "./review-scheduled-payments", "text!./review-scheduled-payments.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});