define(["text!./create-fund-request.html", "./create-fund-request", "text!./create-fund-request.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});