define(["text!./payment-international.html", "./payment-international", "text!./payment-international.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});