define(["text!./review-scan-to-pay.html", "./review-scan-to-pay", "text!./review-scan-to-pay.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});