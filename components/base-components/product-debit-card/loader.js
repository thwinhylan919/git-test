define(["text!./product-debit-card.html", "./product-debit-card", "text!./product-debit-card.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});