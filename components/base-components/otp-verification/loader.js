define(["text!./otp-verification.html", "./otp-verification", "text!./otp-verification.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});