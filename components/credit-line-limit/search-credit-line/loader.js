define(["text!./search-credit-line.html", "./search-credit-line", "text!./search-credit-line.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});