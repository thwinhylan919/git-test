define(["text!./approved-log.html", "./approved-log", "text!./approved-log.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});