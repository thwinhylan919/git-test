define(["text!./maker-work-box.html", "./maker-work-box", "text!./maker-work-box.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});