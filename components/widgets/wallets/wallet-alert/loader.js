define(["text!./wallet-alert.html", "./wallet-alert", "text!./wallet-alert.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});