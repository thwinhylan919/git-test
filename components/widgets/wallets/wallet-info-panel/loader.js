define(["text!./wallet-info-panel.html", "./wallet-info-panel", "text!./wallet-info-panel.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});