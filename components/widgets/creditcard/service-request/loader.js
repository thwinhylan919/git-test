define(["text!./service-request.html", "./service-request", "text!./service-request.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});