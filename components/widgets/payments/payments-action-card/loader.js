define(["text!./payments-action-card.html", "./payments-action-card", "text!./payments-action-card.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});