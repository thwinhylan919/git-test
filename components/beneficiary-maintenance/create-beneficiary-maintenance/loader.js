define(["text!./create-beneficiary-maintenance.html", "./create-beneficiary-maintenance", "text!./create-beneficiary-maintenance.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});