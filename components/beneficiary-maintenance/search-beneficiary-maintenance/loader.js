define(["text!./search-beneficiary-maintenance.html", "./search-beneficiary-maintenance", "text!./search-beneficiary-maintenance.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});