define(["text!./access-point-create.html", "./access-point-create", "text!./access-point-create.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});