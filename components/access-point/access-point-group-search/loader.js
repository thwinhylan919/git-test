define(["text!./access-point-group-search.html", "./access-point-group-search", "text!./access-point-group-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});