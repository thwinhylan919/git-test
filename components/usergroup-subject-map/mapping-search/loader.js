define(["text!./mapping-search.html", "./mapping-search", "text!./mapping-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});