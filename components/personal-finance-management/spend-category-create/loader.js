define(["text!./spend-category-create.html", "./spend-category-create", "text!./spend-category-create.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});