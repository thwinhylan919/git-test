define(["text!./tax-deduction-at-source.html", "./tax-deduction-at-source", "text!./tax-deduction-at-source.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});