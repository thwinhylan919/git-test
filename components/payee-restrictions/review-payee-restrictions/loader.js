define(["text!./review-payee-restrictions.html", "./review-payee-restrictions", "text!./review-payee-restrictions.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});