define(["text!./limit-search.html", "./limit-search", "text!./limit-search.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});