define(["text!./review-create-limit.html", "./review-create-limit", "text!./review-create-limit.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});