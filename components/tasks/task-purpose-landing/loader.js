define(["text!./task-purpose-landing.html", "./task-purpose-landing", "text!./task-purpose-landing.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});