define(["text!./wallet-signup.html", "./wallet-signup", "text!./wallet-signup.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});