define(["text!./wallet-product.html", "./wallet-product", "text!./wallet-product.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});