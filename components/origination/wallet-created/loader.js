define(["text!./wallet-created.html", "./wallet-created", "text!./wallet-created.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});