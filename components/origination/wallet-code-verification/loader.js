define(["text!./wallet-code-verification.html", "./wallet-code-verification", "text!./wallet-code-verification.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});