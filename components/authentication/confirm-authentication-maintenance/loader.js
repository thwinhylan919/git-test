define(["text!./confirm-authentication-maintenance.html", "./confirm-authentication-maintenance", "text!./confirm-authentication-maintenance.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});