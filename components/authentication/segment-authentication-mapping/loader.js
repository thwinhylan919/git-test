define(["text!./segment-authentication-mapping.html", "./segment-authentication-mapping", "text!./segment-authentication-mapping.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});