define(["text!./virtual-structure-tab.html",
    "./virtual-structure-tab"
], function(template, viewModel) {
    "use strict";

    return {
        viewModel: viewModel,
        template: template
    };
});