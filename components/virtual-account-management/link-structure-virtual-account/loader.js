define(["text!./link-structure-virtual-account.html",
  "./link-structure-virtual-account"
  ], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});
