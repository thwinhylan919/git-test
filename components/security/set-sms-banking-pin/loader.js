define(["text!./set-sms-banking-pin.html", "./set-sms-banking-pin", "text!./set-sms-banking-pin.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});