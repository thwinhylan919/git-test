define(["text!./guarantee-nav-bar.html", "./guarantee-nav-bar", "text!./guarantee-nav-bar.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});