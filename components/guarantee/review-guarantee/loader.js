define(["text!./review-guarantee.html", "./review-guarantee", "text!./review-guarantee.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});