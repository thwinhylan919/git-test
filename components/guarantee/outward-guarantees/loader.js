define(["text!./outward-guarantees.html", "./outward-guarantees", "text!./outward-guarantees.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});