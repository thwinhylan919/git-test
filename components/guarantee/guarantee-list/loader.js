define(["text!./guarantee-list.html", "./guarantee-list", "text!./guarantee-list.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});