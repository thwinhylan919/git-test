define(["text!./view-bank-guarantee.html", "./view-bank-guarantee", "text!./view-bank-guarantee.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});