define(["text!./export-amendment.html", "./export-amendment", "text!./export-amendment.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});