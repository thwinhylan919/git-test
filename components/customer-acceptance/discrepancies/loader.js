define(["text!./discrepancies.html", "./discrepancies", "text!./discrepancies.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});