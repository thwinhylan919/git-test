define(["text!./customer-acceptance-nav-bar.html", "./customer-acceptance-nav-bar", "text!./customer-acceptance-nav-bar.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});