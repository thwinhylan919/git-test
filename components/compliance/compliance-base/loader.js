define(["text!./compliance-base.html", "./compliance-base", "text!./compliance-base.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});