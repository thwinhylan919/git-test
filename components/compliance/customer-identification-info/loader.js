define(["text!./customer-identification-info.html", "./customer-identification-info", "text!./customer-identification-info.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});