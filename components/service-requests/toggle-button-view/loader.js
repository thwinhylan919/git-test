define(["text!./toggle-button-view.html", "./toggle-button-view", "text!./toggle-button-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});