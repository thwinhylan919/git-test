define(["text!./section-header-view.html", "./section-header-view", "text!./section-header-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});