define(["text!./toggle-button-control.html", "./toggle-button-control", "text!./toggle-button-control.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});