define(["text!./text-box-view.html", "./text-box-view", "text!./text-box-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});