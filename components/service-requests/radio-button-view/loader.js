define(["text!./radio-button-view.html", "./radio-button-view", "text!./radio-button-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});