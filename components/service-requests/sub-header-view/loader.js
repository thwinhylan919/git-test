define(["text!./sub-header-view.html", "./sub-header-view", "text!./sub-header-view.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});