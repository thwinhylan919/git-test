define(["text!./user-search-admin.html", "./user-search-admin", "text!./user-search-admin.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});