define(["text!./review-funds-transfer.html", "./review-funds-transfer", "text!./review-funds-transfer.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});