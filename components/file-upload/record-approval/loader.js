define(["text!./record-approval.html", "./record-approval", "text!./record-approval.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});