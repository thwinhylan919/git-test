define(["text!./deposit-type.html", "./deposit-type", "text!./deposit-type.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});