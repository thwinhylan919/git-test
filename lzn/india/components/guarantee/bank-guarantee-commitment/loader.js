define(["text!./bank-guarantee-commitment.html", "./bank-guarantee-commitment", "text!./bank-guarantee-commitment.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});