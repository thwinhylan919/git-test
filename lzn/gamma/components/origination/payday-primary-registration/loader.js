define(["text!./payday-primary-registration.html", "./payday-primary-registration"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});