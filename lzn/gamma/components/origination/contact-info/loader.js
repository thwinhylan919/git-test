define(["text!./contact-info.html", "./contact-info", "text!./contact-info.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});