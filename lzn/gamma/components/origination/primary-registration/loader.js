define(["text!./primary-registration.html", "./primary-registration", "text!./primary-registration.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});