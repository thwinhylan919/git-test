define(["text!./tenure-since.html", "./tenure-since", "text!./tenure-since.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});