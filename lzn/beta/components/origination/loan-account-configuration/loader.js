define(["text!./loan-account-configuration.html", "./loan-account-configuration"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});