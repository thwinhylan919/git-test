define(["text!./card-balance-transfer-input.html", "./card-balance-transfer-input"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});