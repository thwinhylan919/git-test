define(["text!./application-status-history.html", "./application-status-history"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});