define(["text!./application-additional-details.html", "./application-additional-details"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});