define(["text!./application-dashboard-actions.html", "./application-dashboard-actions"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});