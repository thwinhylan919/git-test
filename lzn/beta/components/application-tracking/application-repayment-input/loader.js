define(["text!./application-repayment-input.html", "./application-repayment-input"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});