define(["text!./application-offer-container.html", "./application-offer-container"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});