define(["text!./confirm-dialog.html", "./confirm-dialog", "text!./confirm-dialog.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});