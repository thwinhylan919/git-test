define(["text!./generic-authentication.html", "./generic-authentication", "text!./generic-authentication.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});