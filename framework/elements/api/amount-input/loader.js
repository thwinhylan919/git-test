define(["text!./amount-input.html", "./amount-input", "text!./amount-input.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});