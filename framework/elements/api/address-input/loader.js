define(["text!./address-input.html", "./address-input", "text!./address-input.json"], function(template, viewModel) {
  "use strict";

  return {
    viewModel: viewModel,
    template: template
  };
});