define([], function () {
    "use strict";

    const attributetransactiontreeLocale = function () {
        return {
            root: {
                heading: {
                    transactiontree: "transaction tree"
                },
                attributeTree: "Tree {attributeName}"
            },
            ar: true,
            fr: true,
            cs: true,
            sv: true,
            en: false,
es :true,
            "en-us": false,
            el: true
        };
    };

    return new attributetransactiontreeLocale();
});